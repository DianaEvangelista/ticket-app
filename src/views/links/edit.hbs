<div class="col-md-6 mx-auto">

    <div class="card">
        <div class="card-body">
            <form action="/links/edit/{{link.id}}" method="POST">
                <div class="form-group">
                    <input required type="text" name="title" class="form-control" placeholder="Título" value="{{link.title}}" autofocus>
                </div>
                
                
                <div class="form-group">
                    <textarea required name="description" rows="2" class="form-control" placeholder="Reporte">{{link.report}}</textarea>
                </div>
                <div class="form-group">
                    <button class="btn btn-success btn-block">
                       Guardar
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>